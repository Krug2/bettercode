/**
 * Settings schema — re-exported from the workspace package
 * `@betterc0de/schema` (`packages/schema/src/settings.ts`), the
 * authoritative source shared with the renderer.
 */
export {
  providerConfigSchema,
  providerSettingsSchema,
  skillSchema,
  mcpServerSchema,
  hookSchema,
  designDefaultsSchema,
  designBriefSchema,
  settingsSchema,
  settingsPatchSchema,
  secretPatchSchema,
  secretStateSchema,
  isSensitiveProviderFieldName,
  defaultSettings,
  type ProviderConfig,
  type ProviderSettings,
  type Skill,
  type McpServer,
  type Hook,
  type DesignDefaults,
  type DesignBrief,
  type Settings,
  type SettingsPatchBody,
  type SecretPatch,
  type SecretState,
  type SecretStorage,
} from "@betterc0de/schema"
