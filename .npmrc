# Registry lookups can fail transiently on developer and hosted-runner DNS.
# Keep npm ci deterministic while allowing short-lived ENOTFOUND failures to
# recover without requiring a manual reinstall.
registry=https://registry.npmjs.org/
fetch-retries=5
fetch-retry-factor=2
fetch-retry-mintimeout=1000
fetch-retry-maxtimeout=30000
